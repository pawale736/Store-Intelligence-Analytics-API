# Store-Intelligence-Analytics-API
Store Intelligence Analytics API is a Spring Boot backend application that ingests retail store events and provides analytics such as visitor count, dwell time, peak hours, zone-wise traffic, and store-specific insights.  The application uses Spring Boot, Spring Data JPA, Hibernate, and MySQL.
